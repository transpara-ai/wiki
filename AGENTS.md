# Repository instructions

- Use the host-installed `transpara-tlc` plugin's `tlc` skill for software
  changes; default to Routine and escalate only under its route rules.
- TLC is external and unpinned here. Installation and updates belong to the
  host; do not pin, install, enable, or copy TLC through repository configuration.
