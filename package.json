{
  "name": "civilization-wiki",
  "version": "0.1.0",
  "private": true,
  "description": "Static Civilization Wiki renderer and visualization smoke tests.",
  "scripts": {
    "build": "python3 compile/build_site.py",
    "test:js": "node --check compile/assets/civilizationArcData.js && node --check compile/assets/civilizationArcNav.js",
    "test:dom": "node tests/arc-dom-smoke.test.js",
    "test:browser": "playwright install chromium && playwright test",
    "verify": "npm run build && npm run test:js && npm run test:dom && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "jsdom": "^27.2.0"
  }
}
